#include "common.h"

vector<instruction> m1(vector<road_act> d, string w){
    if (w == "steer"){
        cout << "steer";
    }
    if (w == "back"){
        cout << "back";
    }
    // instruction test_instruction = {
    //     0,
    //     0
    // };
    // vector<instruction> _test_vec;
    // _test_vec.push_back(test_instruction);
    // return _test_vec;
}